import { CardsFooter } from "../template";

const FooterSection = () => {
  return (
    <section id="about" className="my-40">
      <CardsFooter />
    </section>
  );
};

export default FooterSection;
