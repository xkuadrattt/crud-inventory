import { FinalCta } from "../fragments";

const FinalCtaSection = () => {
  return (
    <section id="finalcta" className="my-20">
      <FinalCta />
    </section>
  );
};

export default FinalCtaSection;
